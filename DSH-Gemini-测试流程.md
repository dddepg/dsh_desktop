# DSH × Gemini（Vertex）接入与全功能测试流程

> 适用：DSH Desktop 0.6.3-beta.1 及以上（含代理透传修复）
> 测试模型：Google Gemini（Vertex Express `AQ.` key，备选 AI Studio `AIza` key）
> 预计耗时：30–45 分钟

---

## 〇、环境准备（一次性）

1. DSH 已升级到 **0.6.3-beta.1 或之后版本**（含代理透传修复）；
2. 网络可访问 Google（国内需系统代理：设置 → 网络代理，或系统环境变量 `HTTPS_PROXY`）；
3. 备好 key：
   - `AQ.` 开头（Vertex Express，建议首选实测）
   - `AIza` 开头（AI Studio，作为 401/403 时的备选）

---

## 一、添加供应商（两条路径，先走 A）

### 路径 A：Vertex Express + OpenAI 兼容层
设置 → 模型 → 添加供应商（自定义）：

| 字段 | 值 |
|---|---|
| ID / 名称 | `google-vertex` |
| **API 协议** | **Chat Completions**（openai-completions）|
| **API 地址** | `https://aiplatform.googleapis.com/v1/publishers/google/models/openai` |
| **API Key** | 你的 `AQ.` key |
| 模型 | `gemini-2.5-flash`、`gemini-2.5-pro` |

### 路径 B（A 返回 401/403 时）：AI Studio + OpenAI 兼容层
| 字段 | 值 |
|---|---|
| API 协议 | Chat Completions |
| **API 地址** | `https://generativelanguage.googleapis.com/v1beta/openai` |
| **API Key** | [aistudio.google.com](https://aistudio.google.com) 免费领取的 `AIza` key |
| 模型 | `gemini-2.5-flash`、`gemini-2.5-pro` |

---

## 二、逐项测试（每项记录：通过 / 失败 + 现象）

### T1 基础对话与流式
- **操作**：选中 gemini-2.5-flash → 发送「用一句话介绍你自己」；
- **预期**：流式逐字输出，内容正常；
- **失败记录**：报错全文 + 是否 401/403/429。

### T2 工具调用
- **操作**：同一会话发「现在几点了？」或「读一下当前目录下的 package.json 的 name 字段」；
- **预期**：模型发起工具调用（时间/读文件）→ 返回结果 → 正常作答；
- **失败记录**：是否出现 `unknown tool`（含具体名字）或「工具调用 name 为空」。

### T3 文件编辑 + 「文件」视图 + 编辑器 diff（三联动）
- **操作**：让 AI「在会话目录创建 `hello.md`，写两行自我介绍」，完成后再说「把第二行改成 XXX」；
- **预期**：
  1. 对话视图顶栏出现「**文件**」标签，列出 `hello.md`；
  2. 「文件」视图点开 `hello.md` 可见按变更历史的 **+/− diff**；
  3. 打开该文件的编辑器预览 → 工具条出现 **`±`** 与 **`≡`** → `≡` 展开按变更倒序的 diff 行（`+` 绿 / `−` 红 / `±` 黄）。

### T4 图片输入（多模态）
- **操作**：粘贴或拖入一张截图，问「图里有什么文字」；
- **预期**：正确识别并回答；
- **失败记录**：报错码与内容（图片链路在 openai 协议下的注入形态）。

### T5 思考档位滑块
- **操作**：把思考强度滑块分别调到最低/最高，各问一个需要推理的问题（如数学题）；
- **预期**：均正常应答；高档回答质量/耗时应有差异；
- **失败记录**：档位切换后是否报错（reasoning_effort 映射）。

### T6 长上下文
- **操作**：让 AI「读取 xxx 大文件并总结要点」（选一个几千行的真实文件）；
- **预期**：正常读取与总结；
- **失败记录**：是否触发上下文超限或超时。

### T7 会话恢复（重启续聊）
- **操作**：完全退出 DSH → 重新打开 → 回到该会话继续提问；
- **预期**：历史消息完整、可继续对话、供应商配置仍在。

### T8 一键重启联动
- **操作**：托盘右键 → 一键重启；
- **预期**：应用退出后 **约 2 秒自动重新拉起**（不再「只退出」），重启后模型与供应商配置仍在。

---

## 三、失败排查速查

| 现象 | 首查 | 次查 |
|---|---|---|
| 401 / 403 | AQ. key 不被 OpenAI 层接受 → **换路径 B（AIza key）** | 系统代理是否劫持了请求 |
| 429 | 免费额度限速 → 换 gemini-2.5-flash 或稍后 | — |
| DNS 挂起 / 请求超时 | **系统代理没配**（0.6.3 已透传代理变量，检查系统级 HTTPS_PROXY） | 端点是否被网络拦截 |
| `unknown tool "xxx"` | 已有归一化兜底；把具体名字发回 | — |
| 图片发送失败 | 该轮模型/协议的图片注入形态 | 换 png 试试 |

**日志位置**（失败时一并截取）：
- Windows：`%APPDATA%\dsh-desktop\logs\dsh-web.log`（内核）+ `desktop.log`（壳）
- macOS：`~/Library/Application Support/dsh-desktop/logs/` 同名文件

---

## 四、结果回填模板

```
T1 基础对话：   通过 / 失败（现象：____）
T2 工具调用：   通过 / 失败（现象：____）
T3 文件三联动： 通过 / 失败（哪一步断：____）
T4 图片输入：   通过 / 失败
T5 思考档位：   通过 / 失败
T6 长上下文：   通过 / 失败
T7 会话恢复：   通过 / 失败
T8 一键重启：   通过 / 失败
使用路径：      A（AQ. key）/ B（AIza key）
模型：          gemini-2.5-flash / pro / 其它：____
```

全部通过 → 该供应商可进内置目录（开箱即选）；有任何失败 → 带上回填模板与日志，逐条定位修复。
